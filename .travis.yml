branches:
  only:
    - master
    - develop

language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
install: "python setup.py -q install"
script: py.test -q --tb short

#Database
addons:
  postgresql: "9.3"

before_script:
  - psql -c 'create database scrom_test;' -U postgres

